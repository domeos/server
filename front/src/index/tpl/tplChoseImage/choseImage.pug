.d-modal-header 选择基础镜像
div.modal-projects
  loading(ng-if="loading")
  .filter-con
    span.tool-align-input 可选择一个基础镜像作为底层镜像
    input.ui-input.ui-input-fill.ui-input-search(ng-model="key.searchKey" placeholder="输入镜像名称搜索")
  .table-container
    table.ui-table-dome
      thead
        tr
          th 镜像名称
          th 镜像版本
          th 仓库地址
          th 镜像描述
          th 操作
      tbody
        tr(ng-repeat="img in imageListFiltered=(imageList|filter:{'imageName':baseImageKey})")
          td(ng-bind="img.imageName")
          td(ng-bind="img.imageTag")
          td(ng-bind="img.registry")
          td(ng-bind="img.description")
          td
            button.ui-btn.ui-btn-sm.ui-btn-bright(ng-click="choseImage(img)") 选择
        tr(ng-if="!imageList || !imageListFiltered.length")
          td(colspan="6").text-center 暂无相关项目
